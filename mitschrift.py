secret = 13
guess = int(input("Guess a number between 1 and 30: "))

Andere Schreibweise:
user_input = input("Guess a number: ")
guess = int(user_input)

if guess == secret:
    Andere Schreibweise:
    guessed_correctly = guess == secret
    if guessed_correctly:
        Einzige mögliche Antwort: Richtig oder Flasch

